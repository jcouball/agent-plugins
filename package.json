{
  "name": "jcouball-agent-plugins",
  "private": true,
  "description": "Tooling for the jcouball agent plugin marketplace. Not published.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "prepare": "husky",
    "lint:manifests": "node scripts/check-manifests.mjs",
    "lint:links": "node scripts/check-links.mjs",
    "lint:actions": "node scripts/check-actions.mjs",
    "lint:markdown": "markdownlint-cli2",
    "lint:commits": "commitlint --from origin/main --to HEAD",
    "test:worktrees": "bash plugins/worktrees/test/run.sh",
    "test:worktrees:mutate": "node plugins/worktrees/test/mutate.mjs",
    "ci": "npm run lint:manifests && npm run lint:links && npm run lint:actions && npm run lint:markdown && npm run lint:commits && npm run test:worktrees && npm run test:worktrees:mutate",
    "sync": "node scripts/sync-plugins.mjs"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.8.0",
    "@commitlint/config-conventional": "^19.8.0",
    "husky": "^9.1.7",
    "markdownlint-cli2": "^0.23.2"
  }
}
