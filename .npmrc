; Refuse to install under a Node too old for any dependency, naming the
; package that wants more. This replaces a hand-maintained engines claim,
; which goes stale the moment a dependency raises its own floor.
engine-strict=true
